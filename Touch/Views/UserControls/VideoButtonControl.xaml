<UserControl
    x:Class="Touch.Views.UserControls.VideoButtonControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Touch.Views.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid x:Name="RootGrid" Background="#80000000">
        <Button x:Name="PlayButton" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource ListButtonStyle}">
            <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="{StaticResource Play}"/>
        </Button>
        <Button x:Name="ReplayButton" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource ListButtonStyle}">
            <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="{StaticResource Refresh}"/>
        </Button>
    </Grid>
</UserControl>
