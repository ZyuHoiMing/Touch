<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Touch.Styles">

    <!-- 窗口宽度临界值 -->
    <x:Double x:Key="NarrowMinWidth">0</x:Double>
    <x:Double x:Key="NormalMinWidth">528</x:Double>
    <x:Double x:Key="WideMinWidth">1056</x:Double>

    <Style
        TargetType="SymbolIcon"
        x:Name="HambugerMenuSymbolIcon">
        <Setter Property="Height" Value="24"/>
        <Setter Property="Width" Value="24"/>
    </Style>

    <Style
        TargetType="TextBlock"
        x:Name="HambugerMenuText">
        <Setter Property="Margin" Value="24,0,0,0"/>
    </Style>

    <Style
        TargetType="CommandBar"
        x:Name="TitleBarStyle">
        <Setter Property="Canvas.ZIndex" Value="100"/>
        <Setter Property="Background" Value="{ThemeResource SystemControlBackgroundBaseLowBrush}"/>
    </Style>

    <Style
        TargetType="TextBlock"
        x:Name="TitleBar_TitleTextStyle">
        <Setter Property="Margin" Value="12,14"/>
    </Style>
    
</ResourceDictionary>
